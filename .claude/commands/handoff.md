---
description: Refresh the Contextive handoff doc to reflect the current state of the project.
argument-hint: "[optional: anything notable to capture this session]"
---

Update `contextive-toolkit/CONTEXTIVE_CLAUDE_CODE_HANDOFF.md` so a fresh session could pick up
exactly where this one leaves off.

Steps:
1. Read the current handoff so you preserve its structure and section order — improve deliberately,
   don't let the format drift.
2. Reconcile it against reality: campaign lineage and which campaign is in progress, what's been
   measured vs pending, the state of the website and which campaigns are live on it, any new tools/
   connectors/plugins, and any decisions made this session.
3. Fold in anything I flag here: $ARGUMENTS
4. Rewrite the handoff in place. Keep the non-negotiables and the verbatim firewall line intact.
5. Show me a short diff summary of what changed.

Do not invent state you can't verify — if you're unsure whether something shipped, say so rather
than asserting it.
